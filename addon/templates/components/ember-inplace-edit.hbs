{{#if isEditing}}
  <form {{action "doneEditing" on="submit"}}>
    {{#if isTypeInput}}
      {{input value=value}}
    {{else}}
      {{textarea value=value}}
    {{/if}}
  </form>
{{else}}
  <span class="edit-box {{if disabled 'edit-box-disabled' ''}}" {{action "startEditing"}}>
    {{#if displayPlaceholder}}
      <div class={{placeholderClass}}>
        {{placeholder}}
      </div>
    {{else}}
      {{text}}
    {{/if}}
  </span>
  <span class="edit hide {{if disabled 'edit-button-disabled' ''}}">
    {{#if editIcon}}
      <a {{action "startEditing"}} class={{editIcon}}></a>
    {{else}}
      <button {{action "startEditing"}}>edit</button>
    {{/if}}
  </span>
{{/if}}
